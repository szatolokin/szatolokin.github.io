include mixin/pug/variables
include mixin/pug/mixins

doctype html
html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")

    title=pageTitle

    block styles
      link(rel="stylesheet", href="css/main.css")

  body
    .wrapper
      input(type="checkbox" id="checkbox").checkbox-input
      label(for="checkbox").checkbox-label Активировать

      .block 123

    block scripts
      script(src="js/modules/jquery.min.js") 
      script(src="js/main.dev.js")